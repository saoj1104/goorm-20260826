# 재테크 명언록 웹앱 기술명세서

**Project Name:** Money Quote / 재테크 명언록  
**Version:** MVP 1.0  
**Target:** 바이브코딩 입문자  
**Tech Stack:** HTML5 + CSS3 + Vanilla JavaScript  
**배포 목표:** GitHub Pages 또는 Vercel  
**개발 원칙:** 작은 MVP → 실행 → 테스트 → 개선

---

# 1. 프로젝트 개요

## 1.1 프로젝트 목표

재테크에 관심 있는 사용자가 매일 한 개 이상의 명언을 쉽고 아름다운 카드 형태로 확인하고, 명언의 의미를 이해하고, 자신의 재테크 행동으로 연결할 수 있도록 하는 웹 애플리케이션을 개발한다.

기존 명언 앱처럼 단순히 문장을 보여주는 것에서 끝나지 않고 다음 구조로 발전시키는 것을 목표로 한다.

```text
재테크 명언
↓
쉬운 해설
↓
핵심 키워드
↓
오늘의 실천
↓
저장 / 공유
```

MVP에서는 AI API를 사용하지 않는다.

미리 준비된 명언 데이터를 JavaScript 또는 JSON으로 관리하여 HTML, CSS, JavaScript만으로 완성한다.

---

# 2. Target User

주요 사용자는 다음과 같다.

- 재테크를 처음 시작하는 사람
- 주식·부동산·저축에 관심 있는 사람
- 매일 짧은 경제·투자 콘텐츠를 보고 싶은 사람
- 긴 재테크 서적보다 핵심 문장을 선호하는 사람
- 명언을 저장하거나 SNS로 공유하고 싶은 사람

특히 재테크 초보자도 이해할 수 있도록 어려운 금융 용어를 최소화한다.

---

# 3. 핵심 사용자 시나리오

사용자가 웹앱에 접속한다.

```text
앱 실행
↓
오늘의 명언 카드 표시
↓
명언 읽기
↓
인물·카테고리 확인
↓
쉬운 해설 보기
↓
오늘의 실천 확인
↓
좋아요 또는 저장
↓
다음 명언 보기
↓
필요 시 공유
```

예:

```text
오늘의 재테크 명언

“가격은 당신이 지불하는 것이고,
가치는 당신이 얻는 것이다.”

Warren Buffett

[가치투자]

쉬운 해설
가격이 싸다고 좋은 투자라는 뜻은 아닙니다.
내가 지불하는 가격보다 더 큰 가치가 있는지를
생각하는 것이 중요합니다.

오늘의 실천
관심 있는 투자 대상 하나를 골라
가격과 가치를 따로 적어보세요.

♡ 저장     ↗ 공유

[다음 명언]
```

---

# 4. MVP 범위

## 4.1 반드시 구현할 기능

MVP에서는 다음 8개 기능을 구현한다.

1. 오늘의 명언 표시
2. 다음 명언 보기
3. 이전 명언 보기
4. 카테고리 표시
5. 쉬운 해설 표시
6. 오늘의 실천 표시
7. 즐겨찾기 저장
8. 명언 공유

---

# 5. MVP에서 제외할 기능

초기 버전에서는 다음 기능을 구현하지 않는다.

```text
회원가입
로그인
서버
데이터베이스
AI API
결제
구독
사용자 계정
클라우드 동기화
Push Notification
관리자 페이지
```

이 기능들은 MVP가 정상 작동한 이후 추가한다.

---

# 6. 기술 스택

## HTML

역할:

```text
화면 구조
명언 카드
버튼
메뉴
카테고리
즐겨찾기 화면
```

사용 기술:

```text
HTML5
Semantic HTML
button
section
header
main
nav
```

---

## CSS

역할:

```text
카드 디자인
레이아웃
글꼴
배경 이미지
그라데이션
반응형 화면
애니메이션
다크 UI
```

사용 기술:

```text
CSS Variables
Flexbox
CSS Grid
Media Query
Transition
Transform
Linear Gradient
Border Radius
Backdrop Filter
```

외부 CSS Framework는 MVP에서 사용하지 않는다.

---

## JavaScript

역할:

```text
명언 데이터 관리
명언 변경
카테고리 필터
즐겨찾기
공유
LocalStorage
UI 업데이트
```

사용 기술:

```text
Vanilla JavaScript
Array
Object
DOM
Event Listener
localStorage
Web Share API
```

---

# 7. 전체 시스템 구조

```text
User
↓
Browser
↓
index.html
↓
style.css
↓
app.js
↓
quotes.js
↓
LocalStorage
```

서버가 없는 Client-Side Application 구조를 사용한다.

---

# 8. 권장 프로젝트 구조

```text
money-quote/
│
├── index.html
│
├── README.md
│
│
├── css/
│   └── style.css
│
├── js/
│   ├── app.js
│   └── quotes.js
│
├── images/
│   ├── backgrounds/
│   │   ├── invest-01.jpg
│   │   ├── invest-02.jpg
│   │   ├── invest-03.jpg
│   │   └── invest-04.jpg
│   │
│   └── icons/
│
└── docs/
    └── SPEC.md
```

입문자의 경우 처음에는 더 단순하게 시작할 수 있다.

```text
money-quote/
├── index.html
├── style.css
├── app.js
└── images/
```

기능이 안정되면 폴더를 분리한다.

---

# 9. 명언 데이터 구조

명언 데이터는 JavaScript 배열로 관리한다.

예:

```javascript
const quotes = [
  {
    id: 1,
    quote: "가격은 당신이 지불하는 것이고, 가치는 당신이 얻는 것이다.",
    quoteEn: "Price is what you pay. Value is what you get.",
    author: "Warren Buffett",
    authorKo: "워런 버핏",
    category: "가치투자",
    explanation:
      "싸다고 좋은 투자는 아닙니다. 가격보다 실제 가치가 높은지를 판단하는 것이 중요합니다.",
    action:
      "관심 있는 투자 대상 하나를 골라 가격과 가치를 따로 적어보세요.",
    background: "images/backgrounds/invest-01.jpg",
    featured: true
  }
];
```

---

# 10. 데이터 필드 정의

| 필드 | 타입 | 설명 |
|---|---|---|
| id | Number | 명언 고유번호 |
| quote | String | 한국어 명언 |
| quoteEn | String | 원문 |
| author | String | 인물 영문 이름 |
| authorKo | String | 인물 한국어 이름 |
| category | String | 명언 카테고리 |
| explanation | String | 쉬운 해설 |
| action | String | 오늘의 실천 |
| background | String | 배경 이미지 |
| featured | Boolean | 추천 여부 |

---

# 11. 콘텐츠 카테고리

초기 20개 명언을 다음 카테고리로 분류한다.

```text
전체
가치투자
장기투자
절약
저축
복리
위험관리
투자심리
분산투자
자기계발
자산관리
```

상단 카테고리는 너무 많아지지 않도록 MVP에서는 다음 6개 정도만 노출한다.

```text
전체
투자
절약
장기
위험관리
마인드
```

---

# 12. UI Design Concept

첨부된 참고 이미지의 디자인 특징을 적용한다.

핵심 디자인 방향:

```text
Mobile First
Large Card UI
Full Image Background
Rounded Corner
Minimal Interface
Large Typography
Dark Overlay
Gradient Card
Favorite
Share
Swipe 느낌의 전환
```

---

# 13. 전체 화면 구성

모바일 화면 기준 구조:

```text
┌─────────────────────┐
│ Money Quote       ⚙ │
│                     │
│ 오늘의 재테크 명언   │
│                     │
│ ┌─────────────────┐ │
│ │                 │ │
│ │   Background    │ │
│ │                 │ │
│ │ 가격은 당신이    │ │
│ │ 지불하는 것이고  │ │
│ │ 가치는 당신이    │ │
│ │ 얻는 것이다.     │ │
│ │                 │ │
│ │ Warren Buffett  │ │
│ │                 │ │
│ │  ♡       ↗      │ │
│ └─────────────────┘ │
│                     │
│ 가치투자             │
│                     │
│ 쉬운 해설            │
│ .................. │
│                     │
│ 오늘의 실천          │
│ .................. │
│                     │
│ ←       ●       →   │
│                     │
│ 홈   탐색   저장      │
└─────────────────────┘
```

---

# 14. 메인 명언 카드 디자인

첨부 이미지처럼 명언이 카드 안에서 가장 강하게 보이도록 한다.

카드 크기:

```text
width: 100%
max-width: 420px
min-height: 520px
border-radius: 28px
```

기본 스타일:

```css
.quote-card {
    position: relative;
    width: 100%;
    min-height: 520px;
    border-radius: 28px;
    overflow: hidden;
    background-size: cover;
    background-position: center;
}
```

---

# 15. 이미지 위 텍스트 가독성

이미지 위에 명언이 잘 보이도록 검정 그라데이션 Overlay를 사용한다.

```css
.quote-card::before {
    content: "";
    position: absolute;
    inset: 0;

    background:
        linear-gradient(
            to bottom,
            rgba(0,0,0,0.15),
            rgba(0,0,0,0.65)
        );
}
```

---

# 16. Typography

명언이 앱의 가장 중요한 콘텐츠이므로 큰 글자를 사용한다.

```css
.quote-text {
    font-size: 30px;
    font-weight: 700;
    line-height: 1.4;
    text-align: center;
}
```

모바일:

```text
26~32px
```

Desktop:

```text
32~40px
```

저자:

```text
16~18px
```

해설:

```text
16~18px
line-height: 1.7
```

---

# 17. 컬러 시스템

기본 UI는 Dark Mode 느낌으로 구성한다.

```css
:root {

    --bg-main: #0f1115;

    --bg-card: #181c23;

    --text-main: #ffffff;

    --text-sub: #a9b0ba;

    --accent: #7c8cff;

    --border: rgba(255,255,255,0.1);

}
```

명언 카드 자체는 다양한 이미지와 그라데이션을 사용한다.

예:

```text
Sunset
Blue Sky
Mountain
Ocean
City
Abstract
Black & White
Finance
Minimal
```

---

# 18. 카드 스타일 3종

사용자가 다른 분위기를 느낄 수 있도록 3가지 카드 스타일을 준비한다.

## Style A — Photo

사진 + 명언

```text
자연
도시
일출
산
바다
```

---

## Style B — Gradient

배경 이미지를 사용하지 않고 Gradient 적용.

예:

```css
background:
linear-gradient(
135deg,
#667eea,
#764ba2
);
```

---

## Style C — Minimal

단색 배경 + Typography 중심.

```text
검정
화이트
베이지
블루
```

향후 사용자가 스타일을 선택하도록 확장할 수 있다.

---

# 19. 홈 화면

상단:

```text
Money Quote

오늘의 재테크 명언
```

메인:

```text
Quote Card
```

하단:

```text
해설
오늘의 실천
```

Navigation:

```text
홈
탐색
저장
```

---

# 20. 탐색 화면

명언을 카테고리별로 볼 수 있다.

예:

```text
Explore

[전체] [투자] [절약] [장기] [위험관리]

┌──────────────┐
│ Buffett      │
│ 가격과 가치   │
└──────────────┘

┌──────────────┐
│ Munger       │
│ 기다림       │
└──────────────┘
```

카드는 CSS Grid로 구성한다.

```css
.quote-grid {
    display: grid;
    grid-template-columns:
    repeat(2, 1fr);

    gap: 16px;
}
```

---

# 21. 즐겨찾기 기능

사용자가 하트 버튼을 누르면 명언 ID를 LocalStorage에 저장한다.

예:

```javascript
favoriteQuotes = [1, 3, 7, 12]
```

저장:

```javascript
localStorage.setItem(
    "favorites",
    JSON.stringify(favoriteQuotes)
);
```

읽기:

```javascript
const favorites =
JSON.parse(
    localStorage.getItem("favorites")
) || [];
```

---

# 22. 공유 기능

모바일 브라우저에서는 Web Share API를 사용한다.

```javascript
navigator.share({
    title: "오늘의 재테크 명언",
    text:
    `${quote.quote}
    
    - ${quote.authorKo}`
});
```

Web Share API를 지원하지 않는 브라우저에서는 클립보드 복사를 실행한다.

```text
명언이 복사되었습니다.
```

Toast 메시지를 보여준다.

---

# 23. 명언 변경 기능

다음 버튼:

```text
→
```

이전 버튼:

```text
←
```

JavaScript:

```javascript
let currentIndex = 0;

function nextQuote() {

    currentIndex++;

    if(currentIndex >= quotes.length) {
        currentIndex = 0;
    }

    renderQuote();
}
```

---

# 24. Random Quote

Random 버튼을 누르면 임의의 명언을 보여준다.

```javascript
function randomQuote() {

    currentIndex =
        Math.floor(
            Math.random() * quotes.length
        );

    renderQuote();
}
```

---

# 25. Daily Quote

매일 다른 명언이 표시되도록 날짜를 이용한다.

예:

```javascript
const today = new Date();

const dayNumber =
Math.floor(
    today.getTime() / 86400000
);

const quoteIndex =
dayNumber % quotes.length;
```

이 방식의 장점:

```text
서버가 필요 없음
API가 필요 없음
매일 다른 명언 표시
같은 날에는 같은 명언 유지
```

---

# 26. 명언 Render 함수

화면 업데이트는 하나의 함수에서 처리한다.

```javascript
function renderQuote() {

    const quote =
        quotes[currentIndex];

    quoteText.textContent =
        quote.quote;

    authorText.textContent =
        quote.authorKo;

    categoryText.textContent =
        quote.category;

    explanationText.textContent =
        quote.explanation;

    actionText.textContent =
        quote.action;

    quoteCard.style.backgroundImage =
        `url(${quote.background})`;

}
```

입문자가 코드를 이해하기 쉽도록 여러 UI 변경을 renderQuote()에 집중시킨다.

---

# 27. 카드 전환 Animation

명언이 바뀔 때 부드러운 효과를 준다.

```css
.quote-card {
    transition:
        transform 0.35s ease,
        opacity 0.35s ease;
}
```

명언 변경 시:

```text
Fade
Slide
Scale
```

중 하나만 사용한다.

MVP에서는 Fade를 추천한다.

---

# 28. Bottom Navigation

모바일 앱처럼 보이도록 하단 Navigation을 고정한다.

```text
⌂
홈

▦
탐색

♡
저장
```

CSS:

```css
.bottom-nav {

    position: fixed;

    bottom: 0;

    left: 0;

    right: 0;

    height: 72px;

    display: flex;

    justify-content:
        space-around;

    align-items: center;

}
```

---

# 29. 반응형 디자인

Mobile First 방식으로 개발한다.

기준:

```text
Mobile
320px ~ 767px

Tablet
768px ~ 1023px

Desktop
1024px 이상
```

Desktop에서는 중앙에 앱 화면을 배치한다.

```css
.app-container {

    width: 100%;

    max-width: 480px;

    margin: 0 auto;

}
```

이렇게 하면 PC에서도 스마트폰 앱처럼 보인다.

---

# 30. 화면 구성

MVP는 페이지를 여러 HTML 파일로 나누지 않는다.

하나의 index.html에서 View를 변경한다.

```text
Home View
Explore View
Favorites View
```

JavaScript가 화면 표시 여부를 변경한다.

예:

```javascript
showView("home")
showView("explore")
showView("favorites")
```

---

# 31. 상태 관리

별도의 상태관리 라이브러리는 사용하지 않는다.

간단한 JavaScript 변수로 관리한다.

```javascript
const state = {

    currentIndex: 0,

    currentCategory:
        "전체",

    favorites: []

};
```

---

# 32. LocalStorage 저장 항목

MVP에서는 다음 정보만 저장한다.

```text
favorites
selectedTheme
lastQuote
```

예:

```text
moneyQuote_favorites
moneyQuote_theme
moneyQuote_lastQuote
```

---

# 33. 접근성

버튼에는 aria-label을 사용한다.

예:

```html
<button
aria-label="즐겨찾기">
♡
</button>
```

이미지에는 명언 텍스트가 별도로 존재하므로 배경 이미지는 장식 요소로 처리한다.

텍스트 대비는 충분히 확보한다.

---

# 34. 이미지 사용 원칙

참고 이미지처럼 감성적인 사진을 사용하되 명언의 가독성을 가장 중요하게 한다.

추천 이미지:

```text
일출
바다
산
도시
건축
책
길
하늘
자연
추상 이미지
```

금융 앱이라고 해서 모든 이미지에 돈, 지폐, 주식 그래프를 사용할 필요는 없다.

재테크 철학과 명언을 전달하는 앱이므로 감성적이고 고급스러운 이미지를 활용한다.

---

# 35. 아이콘

초기에는 별도의 아이콘 라이브러리를 사용하지 않고 Unicode 또는 SVG를 사용한다.

예:

```text
♡
♥
←
→
↗
☰
```

향후 Lucide Icons 등을 사용할 수 있다.

---

# 36. 에러 처리

이미지가 없는 경우 기본 Gradient를 표시한다.

```javascript
image.onerror = () => {

    quoteCard.classList.add(
        "default-gradient"
    );

};
```

LocalStorage 데이터가 손상된 경우 빈 배열로 초기화한다.

```javascript
try {

   favorites =
   JSON.parse(
       localStorage.getItem(
           "favorites"
       )
   ) || [];

}
catch {

   favorites = [];

}
```

---

# 37. 성능 기준

초기 로딩:

```text
3초 이내
```

이미지:

```text
WebP 권장
300~800KB 이하
```

초기 명언 데이터:

```text
20~50개
```

외부 라이브러리:

```text
MVP에서는 사용하지 않음
```

---

# 38. 보안

MVP에는 API Key가 존재하지 않는다.

따라서 다음 정보는 HTML 또는 JavaScript에 넣지 않는다.

```text
API Key
Password
Token
개인정보
```

향후 AI API를 연결할 경우:

```text
Browser
↓
Backend
↓
AI API
```

구조로 변경한다.

API Key를 JavaScript에 직접 넣어서는 안 된다.

---

# 39. MVP 개발 순서

바이브코딩에서는 한 번에 전체 앱을 만들지 않는다.

## STEP 1

기본 화면 생성

```text
index.html
style.css
```

목표:

```text
명언 카드 하나 표시
```

---

## STEP 2

명언 데이터 생성

```text
quotes.js
```

명언 20개 등록.

---

## STEP 3

JavaScript 연결

```text
app.js
```

명언 표시.

---

## STEP 4

다음 / 이전 기능

```text
Previous
Next
```

---

## STEP 5

즐겨찾기

```text
localStorage
```

---

## STEP 6

공유

```text
Web Share API
```

---

## STEP 7

카테고리

```text
투자
절약
장기
위험관리
```

---

## STEP 8

반응형 UI

```text
Mobile
Desktop
```

---

## STEP 9

테스트

Chrome DevTools에서 테스트.

---

## STEP 10

배포

```text
GitHub
↓
GitHub Pages
```

또는

```text
GitHub
↓
Vercel
```

---

# 40. MVP 완료 조건

다음 항목을 모두 만족하면 MVP를 완료한 것으로 한다.

- [ ] 앱이 정상 실행된다.
- [ ] 명언이 카드 형태로 표시된다.
- [ ] 배경 이미지가 표시된다.
- [ ] 명언 저자가 표시된다.
- [ ] 명언 카테고리가 표시된다.
- [ ] 쉬운 해설이 표시된다.
- [ ] 오늘의 실천이 표시된다.
- [ ] 다음 명언을 볼 수 있다.
- [ ] 이전 명언을 볼 수 있다.
- [ ] 즐겨찾기를 저장할 수 있다.
- [ ] 페이지를 닫아도 즐겨찾기가 유지된다.
- [ ] 명언을 공유할 수 있다.
- [ ] 모바일에서 정상적으로 표시된다.
- [ ] PC에서도 정상적으로 표시된다.
- [ ] GitHub에 코드가 저장되어 있다.
- [ ] 서비스 URL로 접속할 수 있다.

---

# 41. MVP 테스트 항목

## 기능 테스트

### Test 01

앱 실행

Expected:

```text
오늘의 명언 표시
```

### Test 02

다음 버튼 클릭

Expected:

```text
다른 명언 표시
```

### Test 03

즐겨찾기 클릭

Expected:

```text
하트 활성화
LocalStorage 저장
```

### Test 04

페이지 새로고침

Expected:

```text
즐겨찾기 유지
```

### Test 05

공유 버튼

Expected:

```text
공유창 또는 복사 기능 실행
```

### Test 06

카테고리 선택

Expected:

```text
선택한 카테고리의 명언만 표시
```

---

# 42. 디자인 품질 체크

디자인은 다음 기준으로 평가한다.

| 항목 | 목표 |
|---|---|
| 명언 가독성 | 매우 높음 |
| 카드 디자인 | 감성적·고급스러움 |
| 모바일 사용성 | 매우 좋음 |
| 버튼 위치 | 직관적 |
| 색상 | 통일감 |
| 이미지 | 명언 분위기와 조화 |
| 여백 | 충분함 |

---

# 43. V1 확장

MVP가 안정되면 다음 기능을 추가한다.

```text
Theme 선택
검색
명언 이미지 저장
오늘의 명언
최근 본 명언
읽은 명언 기록
카테고리별 추천
Dark / Light Mode
```

---

# 44. V2 — AI 연결

다음 단계에서는 AI를 연결할 수 있다.

```text
사용자
↓
관심분야 선택
↓
명언 검색
↓
AI 해설
↓
오늘의 실천 생성
↓
사용자 피드백
```

예:

```text
나는 부동산에 관심 있어
```

AI:

```text
오늘은 부동산 투자 관점에서
워런 버핏의 가치투자 명언을
소개해 드리겠습니다.
```

---

# 45. V3 — 명언 AI Agent

장기적으로는 다음 구조를 목표로 한다.

```text
User
↓
관심분야
↓
재테크 목표
↓
AI Agent
↓
Quote Knowledge
↓
Personalization
↓
Daily Quote
↓
Explanation
↓
Action
↓
User Record
↓
Memory
```

---

# 46. 향후 수익모델

무료 버전:

```text
오늘의 명언
명언 20~100개
즐겨찾기
공유
```

Premium:

```text
전체 명언
프리미엄 Theme
개인화 추천
AI 해설
AI 음성
광고 제거
```

AI Premium:

```text
재테크 관심사 분석
맞춤 명언
행동 코칭
목표관리
Weekly Report
```

---

# 47. 핵심 차별화

일반 명언 앱:

```text
Quote
```

본 서비스:

```text
Quote
+
Explanation
+
Action
```

AI 버전:

```text
Quote
+
Explanation
+
Action
+
Personalization
+
Memory
```

따라서 이 서비스는 단순한 명언 모음이 아니라

**“재테크 철학을 매일 하나씩 배우고 행동으로 연결하는 서비스”**

를 지향한다.

---

# 48. 개발 원칙

이 프로젝트에서 가장 중요한 원칙은 다음과 같다.

```text
처음부터 완벽하게 만들지 않는다.

명언 카드 1개
↓
20개 데이터
↓
다음 버튼
↓
즐겨찾기
↓
공유
↓
배포
↓
사용
↓
개선
```

AI Agent는 그 다음 단계다.

먼저 HTML, CSS, JavaScript만으로 실제 사용할 수 있는 작은 서비스를 완성한다.

---

# 49. 최종 MVP 구조

```text
HTML
│
├─ Header
│
├─ Quote Card
│
├─ Quote
│
├─ Author
│
├─ Favorite
│
├─ Share
│
├─ Explanation
│
├─ Action
│
└─ Bottom Navigation
│
CSS
│
├─ Mobile First
├─ Card UI
├─ Image Background
├─ Gradient
├─ Dark Theme
└─ Responsive
│
JavaScript
│
├─ Quote Data
├─ Daily Quote
├─ Previous
├─ Next
├─ Random
├─ Favorite
├─ Category
├─ Share
└─ LocalStorage
```

---

# 50. 한 문장 기술 정의

> **HTML, CSS, Vanilla JavaScript만을 사용하여 재테크 명언·쉬운 해설·오늘의 실천을 감성적인 카드 UI로 제공하고, 즐겨찾기와 공유 기능을 지원하는 모바일 우선 웹 애플리케이션을 구현한다.**